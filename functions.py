#page( functions )    Version 1.09     9-23-21

"""def save_():
    with open( "vars.txt", "w" ) as file:
        file.write( str( init.Player.x ) + "\n" )
        file.write( str( init.Player.y ) + "\n" )
        file.write( str( init.Player.health ) + "\n" )
        file.write( str( init.Player.level ) + "\n" )
        file.write( str( init.Player.experience )+ "\n" )
        for items in init.Player.inventory:
            file.write( str( items.name_ ) + ',')
        file.write("\n")
        file.write(str( init.Player.pclass ) + "\n" )
        file.write( str( init.Player.gold ))
    file.close()
save_()"""

"""def load_():
    with open( "vars.txt", "r" ) as file:
        init.Player.x = int( file.readline() )
        init.Player.y = int( file.readline() )
        init.Player.health = int( file.readline() )
        init.Player.level = int( file.readline() ) 
        init.Player.experience = int( file.readline() )
        init.Player.inventory = file.readline()
        init.Player.pclass = str( file.readline())
        init.Player.gold = int( file.readline() )
    file.close()
load_()"""
